server:
  port: ${PORT:7779}
  servlet:
    context-path: /api
management:
  endpoints:
    web:
      exposure:
        include: '*'
  server:
    port: ${ACTPORT:0}
gateway:
  ip: ${GATEWAY:localhost}
spring:
  application:
    name: ${NAME:cards-service}
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:mariadb://beardtrustdb.cibsfknyd6zy.us-east-2.rds.amazonaws.com/BeardTrust_Dev
    username: basileios
    password: EBzrA8x6OcRfZL00dzG9uND6yXANpMetzr7K
    driver-class-name: org.mariadb.jdbc.Driver
